<!DOCTYPE html>
html(lang="en")
    head
        include /head.pug
        title hi
    body
        include /site-header.pug
        p.
            here we are
            #{greeting}
            #{nested.whyNot}
